#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl "\n";

int m, n;
vector<int> t, z, y;

bool isGood(int x)
{
}
signed main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);

    cin >> m >> n;
    t.resize(n);
    z.resize(n);
    y.resize(n);

    for (int i = 0; i < n; i++)
    {
        cin >> t[i];
        cin >> z[i];
        cin >> y[i];
    }
}