
#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, x;
    cin >> x >> a >> b >> c;

    if (a <= x && b <= x && c <= x)
    {
        cout << "OK" << endl;
    }
    else
    {
        cout << "ERROR" << endl;
    }
}