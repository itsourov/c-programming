#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n, m;
    cin >> n >> m;

    int firstProductprice[n], secondProductprice[n];

    for (int i = 0; i < n; i++)
    {
        cin >> firstProductprice[i];
    }
    for (int i = 0; i < n; i++)
    {
        cin >> secondProductprice[i];
    }
}