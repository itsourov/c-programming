#include <bits/stdc++.h>
using namespace std;
#define int long long
#define endl "\n"
bool possible(int arr[], int mid)
{
}
void solve()
{

    int n, c;
    cin >> n >> c;
    int arr[n];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[i];
    }
    int l = 1, r = 1e6;
    while (l <= r)
    {
        int mid = (l + r) / 2;
    }
}
signed main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int t = 1;
    cin >> t;

    while (t--)
    {
        solve();
    }
}